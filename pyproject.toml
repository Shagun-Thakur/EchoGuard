[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "echoguard"
version = "0.1.0"
description = "Research project for machine audio anomaly detection"